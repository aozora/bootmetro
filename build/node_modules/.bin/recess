#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../recess/bin/recess" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../recess/bin/recess" "$@"
  ret=$?
fi
exit $ret
